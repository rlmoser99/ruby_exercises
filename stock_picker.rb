def stock_picker array
    array.each do |num|
        puts num
    end
end

stock_picker([17,3,6,9,15,8,6,1,10])

# Need to find the largest number
# Need to find the smallest number BEFORE the largest number